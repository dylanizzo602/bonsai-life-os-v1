/* Habits table layout constants: shared by HabitTable (1.0) and HabitTableV1 (1.1) */

/** Base pixel size for date columns (square cells); also used for row height and touch targets */
export const DATE_COLUMN_WIDTH_PX = 44

/** Width of the habit name column */
export const HABIT_COLUMN_WIDTH_PX = 140

/** Width of the streak column */
export const STREAK_COLUMN_WIDTH_PX = 100
